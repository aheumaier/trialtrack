<%= form_for(@trials_question) do |f| %>
  <% if @trials_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trials_question.errors.count, "error") %> prohibited this trials_question from being saved:</h2>

      <ul>
      <% @trials_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br>
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :question %><br>
    <%= f.text_field :question %>
  </div>
  <div class="field">
    <%= f.label :trial %><br>
    <%= f.text_field :trial %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
